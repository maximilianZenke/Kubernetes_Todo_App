apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  selector:
    io.kompose.service: frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 32000
  type: NodePort
